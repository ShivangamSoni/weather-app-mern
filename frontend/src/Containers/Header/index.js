// Components
import Logo from "../../Components/Logo";
import SearchBar from "../SearchBar";

const Header = () => {
  return (
    <header>
      <Logo />
      <SearchBar />
    </header>
  );
};

export default Header;
