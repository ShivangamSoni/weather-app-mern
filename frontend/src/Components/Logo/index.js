import { Container, BrandName } from "./StyledComponents";

const Logo = () => {
  return (
    <Container>
      <BrandName>The Weather App</BrandName>
    </Container>
  );
};

export default Logo;
